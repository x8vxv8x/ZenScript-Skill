# 数组 CraftTweaker API 参考

> Mod ID: `minecraft`
> 前置条件: 无
> 导入: 无需导入

数组基础语法。

---

## 数组操作

```zenscript
var arr as int[] = [10, 20, 30];
arr[0];           // 访问
arr[1] = 25;      // 修改
arr += 40;        // 追加
arr.length;       // 长度
```
