# StringList（字符串列表）

> 需安装 ZenUtils
> 导入: `import mods.zenutils.StringList;`

---

## 方法

| 方法 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `StringList.create(String[])` | String[] | StringList | 创建字符串列表 |
